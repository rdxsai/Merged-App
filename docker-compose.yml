version: '3.8'

services:
  chromadb:
    image: chromadb/chroma:0.4.24
    container_name: socratic_chroma
    volumes:
      - chroma_data:/chroma/chroma
    ports:
      - "8000:8000"
    restart: unless-stopped
  
volumes:
  chroma_data: